# Channel-independent application tools

KaspaLinks exposes creator-owned use cases through a shared TypeScript application layer. Website and channel adapters supply only a server-authenticated ActorContext, so ownership cannot be supplied by Telegram messages, AI output, or public request bodies.
