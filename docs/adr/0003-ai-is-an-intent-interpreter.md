# AI is a validated intent interpreter

AI may translate short German or English text into a strict structured intent, but it receives no Application Tools and cannot execute mutations. Mutating intents become expiring Drafts and run only after Telegram confirmation under the connected Creator's server-derived ActorContext.
